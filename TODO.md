# TODO — File Explorer UI (Fitur #1)

## Steps
- [x] 1. Fix `frontend/src/components/explorer/FileExplorer.jsx` — remove duplicate `export default function FileExplorer()` declaration and duplicate `useEffect`, leaving one complete correct implementation.
- [x] 2. Add `{"appId":"app:file-explorer"}` entry to `frontend/public/metadata/navigation.json` (Data & Capability group).
- [x] 3. Verify `AppRegistry.js` and `system.json` (already correct — no change needed).
- [ ] 4. Provide metadata/sidebar entry guide as final deliverable.
- [ ] 5. Test via `npm run desktop`.
